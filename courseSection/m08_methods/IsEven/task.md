<div class="hint" title="Practice topics">
  <i style="padding-left: 40px;">method with return value and params</i>
</div>

Complete the method called `isEven()` that will take an `int`argument and return if the given number is even as a `boolean`.

If the number is even the method returns `true`, but if the number is odd the method returns `false`.

The `isEven()` method is being called in the main method and given an argument.

    Examples:
      isEven(4) -> true
      isEven(7) -> false

<div class="hint">
  <i style="padding-left: 40px;">return</i>
</div>
